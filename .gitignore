node_modules
# Keep environment variables out of version control
.env
# Caddy/DuckDNS env for local `demo:start:caddy` — holds the DuckDNS token
.env.caddy

/generated/prisma

# TypeScript incremental build cache — stale copies cause "Cannot find module"
# after `rimraf dist`, so it must never be committed.
*.tsbuildinfo

# ngrok config may contain an auth token
ngrok.yml

# demo runtime artifacts
logs/
.demo-pids.json
